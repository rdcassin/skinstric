import { DemoOutputChangeProps } from "./types";

const DemoOutputChange = ({ label1, action1, label2, action2 }: DemoOutputChangeProps) => {
    return (
        <div>DEMO OUTPUT CHANGE</div>
    );
}
 
export default DemoOutputChange;